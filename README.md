# gsexp

`gsexp` is a tiny C++20 S-expression parser/writer helper for Gubsy-derived
libraries.

It is intentionally not a Lisp interpreter. It parses small config/data files
into plain values and reports line/column diagnostics.

## Build

```sh
./scripts/build.sh
```

## Run Demo

```sh
./scripts/run.sh
```
